id = "fixture/logmel-embed"
version = "0.0.0"
title = "Synthetic log-mel-input embedding model (test fixture)"
license = "CC0-1.0"

[model]
kind = "embedding"
format = "onnx"
file = "model.onnx"

[input]
modality = "audio"
representation = "log_mel"
sample_rate_hz = 16000
n_mels = 80
n_fft = 400
hop_length = 160

[output]
tier_kind = "continuous_vector"
channels = 8
