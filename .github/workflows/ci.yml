name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # The gate lives in a reusable workflow (gate.yml) so CI and both
  # release workflows run the exact same checks — see gate.yml's header.
  gate:
    uses: ./.github/workflows/gate.yml
